


from setuptools import setup

setup(
      name = "datasciencehelper",
      version = "1.5.0",
      description = "A helper library that saves time.",
      py_modules = ["DataScienceHelper"],
      package_dir = {"": "src"},
      )