from setuptools import setup

setup(name='saman_distributions',
      version='0.1',
      description='Gaussian distributions class',
      packages=['saman_distributions'],
      zip_safe=False,
      author='Saman'
     )
