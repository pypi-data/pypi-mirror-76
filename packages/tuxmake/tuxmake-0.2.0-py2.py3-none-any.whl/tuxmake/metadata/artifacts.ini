[meta]
order=10

[types]
modules = linelist
dtbs= linelist

[commands]
modules = test -f {build_dir}/modules.tar.gz && tar taf {build_dir}/modules.tar.gz | grep -v '/$'
dtbs    = test -f {build_dir}/dtbs.tar.gz    && tar taf {build_dir}/dtbs.tar.gz    | grep -v '/$'
