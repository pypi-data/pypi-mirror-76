[tool.poetry]
name = "BiasedClassifier"
version = "0.3.1"
description = ""
authors = ["Rodrigo Parra <contact@rodrigo-parra.com>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/rparraca/BiasedClassifier"
homepage = "https://rparraca.github.io/BiasedClassifier/"
keywords = ["imbalanced", "classification", "random forest"]

include = [
    "LICENSE",
]

[tool.poetry.dependencies]
python = "^3.7"
scikit-learn = "^0.23.2"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = "^19.10b0"
pandas = "^1.1.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
